package src.main.domain.decision;

public enum EventType {

        PHASE_START,
        TURN_START,
        TURN_END,
        DICE_ROLLED,
        EVENT_DIE_ROLLED,
        PRODUCTION_DIE_ROLLED,
        MESSAGE,
        ERROR,
        GAME_OVER

}
